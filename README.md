# 🤖 Auto Job Finder & Apply

A fully autonomous AI agent that monitors job sources in **real-time** (every 2 minutes), evaluates jobs against your profile, generates tailored CVs and cover letters using AI, applies **instantly** when a matching job is posted, captures proof screenshots, and sends you WhatsApp notifications — all on autopilot.

## ✨ Features

- **Real-Time Monitoring** — Checks all job sources every 2 minutes (not every 6 hours). When a new matching job is posted, the agent applies INSTANTLY — giving you a huge advantage over candidates who apply hours later
- **AI-Powered CV & Cover Letters** — Each CV/cover letter is generated by AI (Pollinations, free, no API key) specifically tailored to the job and company
- **Multi-Source Job Discovery** — Scrapes LinkedIn, RemoteOK, Remotive, WeWorkRemotely, Jobspresso, and **Freelancer.com** (freelance gigs/projects)
- **Smart Job Evaluation** — Scores each job against your profile (skills, experience, location preferences)
- **WhatsApp Notifications** — Sends you a WhatsApp message every time it applies to a job (no paid API, uses WhatsApp Web automation)
- **Proof of Application** — Captures a screenshot of the job page after applying, so you can verify the application was actually made
- **Web Dashboard** — Set up profile, connect WhatsApp, view all applications (with proof screenshots) and activity
- **No API Keys Needed** — AI (Pollinations) and WhatsApp (Web automation) both work without any paid API keys

## 🚀 Quick Start

```bash
# Clone the repo
git clone https://github.com/Hadi990op/Auto-job-Finder-Apply.git
cd Auto-job-Finder-Apply

# Run setup (as root)
sudo ./setup.sh
```

That's it. The dashboard will be live at `http://localhost:9300`.

### One-Line Install (no clone needed)

```bash
curl -sL https://raw.githubusercontent.com/Hadi990op/Auto-job-Finder-Apply/main/quick-install.sh | sudo bash
```

## 📋 Setup After Install

1. **Open the dashboard** → `http://localhost:9300`
2. **Set up your profile** → Go to `/profile` — enter your name, skills, experience, job titles, preferred locations, and auto-apply threshold
3. **Connect WhatsApp** → Go to `/whatsapp` — enter your phone number, get a pairing code, enter it on your phone's WhatsApp → Settings → Linked Devices
4. **Upload your base CV** (optional) → Go to `/profile` — upload your existing CV. The AI will use it as context to generate better tailored CVs.
5. **Done!** The agent monitors all job sources every 2 minutes and applies instantly when a matching job is posted. You'll get WhatsApp notifications for every application.

## 🏗️ Architecture

```
┌─────────────────────────────────────────────────┐
│                  Web Dashboard                    │
│              (FastAPI, port 9300)                  │
├─────────────────────────────────────────────────┤
│  /profile  /jobs  /applications  /whatsapp  /     │
└──────────────┬──────────────────┬────────────────┘
               │                  │
    ┌──────────▼────────┐  ┌─────▼──────┐
    │  Real-Time Monitor  │  │  WhatsApp   │
    │   (every 2 min)     │  │  (Playwright)│
    │                    │  │             │
    │  1. Scan all sources│  │  notifications│
    │  2. Find NEW jobs   │  │             │
    │  3. Evaluate fit   │  └─────────────┘
    │  4. AI generate CV │
    │  5. AI cover letter │
    │  6. Apply INSTANTLY │
    │  7. Screenshot proof│
    │  8. Send WhatsApp   │
    └────────────────────┘
```

## 📁 Project Structure

```
Auto-job-Finder-Apply/
├── setup.sh              # One-command installer
├── uninstall.sh          # Remove everything
├── requirements.txt      # Python dependencies
├── README.md             # This file
├── src/                  # All source code
│   ├── app.py            # Web dashboard (FastAPI)
│   ├── agent.py          # Autonomous agent loop
│   ├── ai_engine.py      # AI CV/cover letter generation (Pollinations)
│   ├── whatsapp.py       # WhatsApp Web automation (Playwright)
│   ├── job_sources.py    # Multi-source job scraper (6 sources + Freelancer.com)
│   ├── linkedin_search.py# LinkedIn job search
│   ├── evaluator.py      # Job fit evaluation
│   ├── generator.py      # Template-based fallback CV/cover letter
│   ├── cv_manager.py     # CV upload/management
│   ├── update_profile.py # Profile update utility
│   ├── reset_and_run.sh  # Reset & restart script
│   └── data/             # Runtime data
│       ├── jobagent.db   # SQLite database (auto-created)
│       ├── cvs/          # Uploaded & generated CVs
│       └── whatsapp/     # WhatsApp session & config
├── deploy/               # Deployment configs
│   ├── job-agent.service       # systemd unit (dashboard)
│   ├── job-agent-loop.service  # systemd unit (agent loop)
│   └── job-agent.caddy         # Caddy reverse proxy config
└── scripts/              # Utility scripts
```

## 🔧 Services

| Service | Description | Port |
|---------|-------------|------|
| `job-agent` | Web Dashboard (FastAPI) | 9300 |
| `job-agent-loop` | Real-time job monitor (checks every 2 min) | — |

### Commands

```bash
# Check status
systemctl status job-agent
systemctl status job-agent-loop

# View logs
journalctl -u job-agent -f
journalctl -u job-agent-loop -f

# Restart
systemctl restart job-agent
systemctl restart job-agent-loop

# Stop
systemctl stop job-agent job-agent-loop
```

## 🤖 AI Details

- **Provider**: [Pollinations AI](https://text.pollinations.ai) — free, no API key, no registration
- Uses OpenAI-compatible API for CV and cover letter generation
- Falls back to template-based generation if AI is unavailable
- Each CV/cover letter is uniquely tailored to the specific job posting

## 📱 WhatsApp Details

- **Method**: Playwright (headless Chromium) automating WhatsApp Web
- **No API key, no paid services** — pure web automation
- Pair once with a code (like WhatsApp Desktop), session persists
- Sends formatted notifications with job title, company, fit score, and link

## 🔌 API Endpoints

| Method | Path | Description |
|--------|------|-------------|
| GET | `/` | Dashboard |
| GET | `/profile` | Profile setup |
| POST | `/api/profile` | Save profile |
| GET | `/jobs` | Browse discovered jobs |
| GET | `/applications` | View applications |
| GET | `/whatsapp` | WhatsApp setup |
| POST | `/api/whatsapp/pair` | Get pairing code |
| POST | `/api/whatsapp/send` | Send test message |
| GET | `/api/whatsapp/status` | Connection status |
| POST | `/api/agent/run` | Trigger agent manually |
| GET | `/api/agent/status` | Agent running status |
| GET | `/activity` | Activity log |
| GET | `/api/proof/{id}` | View application proof screenshot |

## 🌐 Public Access (Optional)

To expose the dashboard publicly with HTTPS using Caddy:

```bash
# Copy the Caddy config
sudo cp deploy/job-agent.caddy /etc/caddy/conf.d/
sudo systemctl reload caddy
```

The dashboard will be available at `https://your-domain/jobs/`

## 📝 Requirements

- Debian 12 / Ubuntu 22.04+ (or any Linux with systemd)
- Python 3.10+
- 2GB RAM minimum (4GB recommended for Chromium)
- Root access (for systemd services)

## 📄 License

MIT — Do whatever you want with it.

## 🤝 How It Works

1. **You set up your profile once** — name, skills, experience, job titles you want, locations you prefer
2. **You connect WhatsApp once** — pair with a code, just like WhatsApp Desktop
3. **The agent monitors all job sources every 2 minutes:**
   - Scans all 6 sources (LinkedIn, RemoteOK, Remotive, WWR, Jobspresso, Freelancer.com) for new jobs
   - Evaluates each new job against your profile (keyword matching + fit score)
   - For matching jobs: AI generates a tailored CV and cover letter **instantly**
   - Applies to the job immediately (don't wait — be first!)
   - Captures a screenshot of the job page as proof of application
   - Sends you a WhatsApp notification with job details
4. **You see everything in the dashboard** — all jobs, applications (with proof screenshots), activity logs

### ⚡ Why Real-Time Matters

When a job is posted, the first applications are the ones recruiters look at most. By checking every 2 minutes (instead of every 6 hours), the agent ensures your application is among the **first** to arrive — often within minutes of the job being posted, while other candidates are still searching manually.

---

Made with ❤️ — Set it up once, let it find and apply to jobs for you in real-time.
